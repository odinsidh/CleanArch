Задача: Жалоба на контент \ Report

type ReportMaster?????? (название тупое, не могу пока лучше придумтаь) struct {
    ReportMasterID
    ContentID 
    ContentType (article\comment)
    Status (новый, в работе, отклонен, жалоба отклонена, жалоба подтверждена)
    Жалобы []Report
    "История по жалобе" []ReportHistory
}

type Report struct {
    Идентификатор Жалобы ReportID
    ReportMasterID
    Кто создал обращение UserID
    Причина жалобы (спам, оскорбление, неприемлимый контент, другое) Reason
    Комментарий от пользователя Message (max 500, необязательное)
    Когда было создано CreatedAt
}

Type "История по жалобе" struct {
    ReportID
    HistoryID
    МодераторID 
    МодераторСообщение
    МодераторНовыйСтатус
    Время
}

UseCase для пользователя
> Создать жалобу(ctx context.Context)
> Посмотреть обращения (получить список своих обращений)
> Посмотреть своё конкретное обращение

UseCase для "модератора"
> Получить список всех жалоб (общий лист)
> Получить список жалоб взятых в работу
> Посмотреть конкретную жалобу (сразу подумать о склейке, если на одну единицу контента прилетело много жалоб)

// Тут уже пошла некая привязка логики
> Взять жалобу в работу (touch)
> Добавить комментарий
> Сменить статус ?
> Забанить контент
> Передать жалобу на рассмотрение другому модератору ? (пока что опционально для реализации)
> Отклонить

UseCase Для админа (тут скромно накидаю, можно и больше методов для админки, для супер пупер админа, но пусть будет пока так)
> посмотреть все жалобы
> вывести жалобы по типу + опциона фильтры (по датам, по модератору, по пользователю)

